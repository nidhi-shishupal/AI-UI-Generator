# AI-UI-Generator
AI UI Generator is a full-stack application that converts natural language prompts into working UI layouts. It builds a structured component plan, renders a live preview, generates JSX code, and explains the interface. Includes version history, prompt suggestions, and a responsive preview panel.
